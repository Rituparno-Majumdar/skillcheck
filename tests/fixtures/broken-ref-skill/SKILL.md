---
name: broken-ref-skill
description: This skill references files that do not exist anywhere in its directory structure on disk.
---

Run the tool:

```bash
scripts/missing.sh
```

Load `reference/absent.md` for context.
